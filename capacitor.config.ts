import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.develap.app',
  appName: 'Visita General Alvear',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
